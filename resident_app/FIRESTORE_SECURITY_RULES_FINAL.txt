rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users to read and write all documents
    // This is a permissive rule for development/testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
